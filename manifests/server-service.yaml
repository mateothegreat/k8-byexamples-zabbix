apiVersion: v1
kind: Service
metadata:
  name: zabbix-server
  namespace: $NS
  labels:
    app: zabbix-server
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: zabbix-server
